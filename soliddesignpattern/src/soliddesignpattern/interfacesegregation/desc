Interface segregation principle : 

The interface state that the larger interface should be split
into smaller ones. because the implement class use the methods 
that are required. we should not force the client to use the
methods that they do not want to use. 

The goal of the interface segregation is the similar to 
single responsibility principle. lets understand the through 
an example.